name: Call Random QTS Query Every 73 Minutes

on:
  schedule:
    # Run at minute 13 of every hour, then skip the next hour (approximately 73 minutes)
    - cron: '13 */2 * * *'
  workflow_dispatch:

jobs:
  call-api-job:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase GET Function 'qtsquery'
        run: |
          curl -f -L -X GET 'https://npwbxlvcfrawolebzawn.supabase.co/functions/v1/qtsquery' \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"